import constants from '@common/constants/layout';

export default {
  neuButton: {
    borderRadius: constants.button.radius,
    backgroundColor: constants.button.background,
    width: constants.button.width,
    height: constants.button.height,
    alignItems: 'center',
    justifyContent: 'center',
  },
};
